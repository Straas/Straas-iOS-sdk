# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!

target 'VideoChat' do
  pod 'SlackTextViewController'
  pod 'StraaS-iOS-SDK', '~> 0.2.0'
end
